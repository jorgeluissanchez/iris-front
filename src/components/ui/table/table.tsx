'use client';

import {
  Table,
  TableHeader,
  TableBody,
  TableColumn,
  TableRow,
  TableCell,
  type TableProps,
  type TableHeaderProps,
  type TableBodyProps,
  type TableColumnProps,
  type TableRowProps,
  type TableCellProps,
} from '@heroui/table';

export { Table, TableHeader, TableBody, TableColumn, TableRow, TableCell, type TableProps, type TableHeaderProps, type TableBodyProps, type TableColumnProps, type TableRowProps, type TableCellProps };
